# .golangci.yml
version: 2

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert
    - unparam
    - gosec

linters-settings:
  errcheck:
    check-blank: true
  govet:
    enable:
      - shadow
  gosec:
    excludes:
      - G104 # Duplicates errcheck
      - G304 # File path from variable (expected in CLI)

run:
  timeout: 5m
  tests: true

issues:
  max-same-issues: 0
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gosec
